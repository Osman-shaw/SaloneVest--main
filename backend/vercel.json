// backend/vercel.json
{
  "version": 2,
  "builds": [
    {
      "src": "api/index.js", // Ensure this path matches your exported server file
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/index.js"
    }
  ]
}